import { HasKeycloakAuthorizationDirective } from './has-keycloak-authorization.directive';

describe('HasKeycloakAuthorizationDirective', () => {
  it('should create an instance', () => {
    const directive = new HasKeycloakAuthorizationDirective();
    expect(directive).toBeTruthy();
  });
});
